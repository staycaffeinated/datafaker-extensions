# This tells lombok this is the root directory.
config.stopBubbling=true

# This adds the @lombok.Generated annotation to 
# lombok-generated classes, which gets them excluded
# from jacoco
lombok.addLombokGeneratedAnnotation=true
